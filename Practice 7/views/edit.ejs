<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Your Profile</title>
</head>
<body class="bg-gradient-to-br from-indigo-500 via-purple-500 to-pink-500 min-h-screen flex items-center justify-center relative">
    <!-- Logout Button -->
    <a href="/logout" class="absolute top-4 right-4 text-red-500 font-medium hover:text-red-700 transition">
        Logout
    </a>

    <!-- Profile Section -->
    <div class="w-full max-w-4xl bg-white rounded-xl shadow-lg p-8">

        <!-- New Post Section -->
        <div class="mb-8">
            <h2 class="text-lg font-semibold text-gray-700 mb-4">Edit your post...</h2>
            <form action="/update/<%= post._id %>" method="post" class="space-y-4">
                <textarea 
                    placeholder="What's on your mind?" 
                    name="content" 
                    rows="4" 
                    class="w-full px-4 py-2 rounded-lg border border-gray-300 shadow-sm focus:ring-indigo-500 focus:border-indigo-500 resize-none"><%= post.content %></textarea>
                <input 
                    type="submit" 
                    value="Update Post" 
                    class="w-full py-3 rounded-lg bg-red-600 text-white font-medium text-lg shadow-md hover:bg-red-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-red-500">
            </form>
        </div>
    </div>
</body>
</html>
